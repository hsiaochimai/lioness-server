CREATE TABLE lioness_projectStatuses (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    email TEXT NOT NULL,
    full_name TEXT NOT NULL,
    role INTEGER REFERENCES lioness_roles(id)ON DELETE CASCADE NOT NULL,
    phone INTEGER NOT NULL,
    
);